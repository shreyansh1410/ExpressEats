import React from 'react'

const SlidingRestaurants = ({filteredRestaurants}) => {
  return (
    <div>
      {filteredRestaurants.map((restaurants) => {
        <div>
          restaurant?.info?.name
        </div>
      })}
    </div>
  )
}

export default SlidingRestaurants
